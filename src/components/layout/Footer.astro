---
import { navigation, siteProfile } from '../../data/site';

const socialLinks = [
  siteProfile.github && { href: siteProfile.github, label: 'GitHub' },
  siteProfile.linkedin && { href: siteProfile.linkedin, label: 'LinkedIn' },
  siteProfile.scholar && { href: siteProfile.scholar, label: 'Google Scholar' },
  siteProfile.orcid && { href: siteProfile.orcid, label: 'ORCID' },
].filter(Boolean) as { href: string; label: string }[];
---

<footer class="site-footer">
  <div class="container footer-grid">
    <div class="footer-identity">
      <p class="footer-mark" aria-hidden="true">NN</p>
      <div>
        <p class="footer-name">{siteProfile.name}</p>
        <p>{siteProfile.position} · {siteProfile.location}</p>
      </div>
    </div>
    <nav aria-label="Footer navigation">
      <ul role="list">
        {
          navigation.slice(1).map(({ href, label }) => (
            <li>
              <a href={href}>{label}</a>
            </li>
          ))
        }
      </ul>
    </nav>
    {
      socialLinks.length > 0 && (
        <nav aria-label="Social links">
          <ul role="list">
            {socialLinks.map(({ href, label }) => (
              <li>
                <a href={href} rel="me">
                  {label}
                </a>
              </li>
            ))}
          </ul>
        </nav>
      )
    }
  </div>
  <div class="container footer-bottom">
    <span>© {new Date().getFullYear()} {siteProfile.name}</span>
    <span>Built with Astro. No tracking.</span>
  </div>
</footer>

<style>
  .site-footer {
    padding-block: clamp(3rem, 7vw, 5rem) 1.5rem;
    border-top: 1px solid var(--color-line-strong);
    color: var(--color-muted);
    font-size: 0.86rem;
  }

  .footer-grid {
    display: grid;
    gap: 2rem;
  }

  .footer-identity {
    display: flex;
    gap: 1rem;
    align-items: start;
  }

  .footer-mark {
    color: var(--color-accent);
    font-family: var(--font-display);
    font-size: 1.45rem;
    font-weight: 600;
    letter-spacing: -0.05em;
    line-height: 1;
  }

  .footer-name {
    margin-bottom: 0.35rem;
    color: var(--color-text);
    font-weight: 750;
  }

  ul {
    display: flex;
    flex-wrap: wrap;
    gap: 0.35rem 1rem;
  }

  a {
    display: inline-flex;
    min-height: 36px;
    align-items: center;
    color: var(--color-text);
    text-decoration: none;
  }

  .footer-bottom {
    display: flex;
    padding-top: 2rem;
    gap: 0.75rem 2rem;
    align-items: center;
    justify-content: space-between;
    border-top: 1px solid var(--color-line);
    margin-top: 2rem;
  }

  @media (min-width: 48rem) {
    .footer-grid {
      grid-template-columns: minmax(16rem, 1fr) auto auto;
    }
  }

  @media (max-width: 36rem) {
    .footer-bottom {
      align-items: start;
      flex-direction: column;
    }
  }
</style>
