---
import { navigation } from '../../data/site';

interface Props {
  pathname: string;
}

const { pathname } = Astro.props;
const isActive = (href: string) => (href === '/' ? pathname === '/' : pathname.startsWith(href));
---

<nav class="desktop-nav" aria-label="Primary navigation">
  <ul class="desktop-list" role="list">
    {
      navigation.map(({ href, label }) => (
        <li>
          <a href={href} aria-current={isActive(href) ? 'page' : undefined}>
            {label}
          </a>
        </li>
      ))
    }
  </ul>
</nav>

<nav class="mobile-nav" aria-label="Mobile navigation">
  <details>
    <summary aria-expanded="false" data-mobile-summary>
      <span>Menu</span>
      <svg aria-hidden="true" viewBox="0 0 16 16" width="16" height="16">
        <path d="m3 6 5 5 5-5"></path>
      </svg>
    </summary>
    <ul class="nav-list" role="list">
      {
        navigation.map(({ href, label }) => (
          <li>
            <a href={href} aria-current={isActive(href) ? 'page' : undefined}>
              {label}
            </a>
          </li>
        ))
      }
    </ul>
  </details>
</nav>

<script>
  document.querySelectorAll<HTMLDetailsElement>('.mobile-nav details').forEach((details) => {
    const summary = details.querySelector<HTMLElement>('[data-mobile-summary]');
    const updateState = () => summary?.setAttribute('aria-expanded', String(details.open));

    details.addEventListener('toggle', updateState);
    details
      .querySelectorAll('a')
      .forEach((link) => link.addEventListener('click', () => details.removeAttribute('open')));
    updateState();
  });
</script>

<style>
  details {
    position: relative;
  }

  .desktop-nav {
    display: none;
  }

  summary {
    display: inline-flex;
    min-height: 44px;
    padding: 0.5rem 0.7rem;
    gap: 0.45rem;
    align-items: center;
    border: 1px solid var(--line);
    border-radius: 7px;
    cursor: pointer;
    font-size: 0.85rem;
    font-weight: 700;
    list-style: none;
  }

  summary::-webkit-details-marker {
    display: none;
  }

  summary svg {
    fill: none;
    stroke: currentColor;
    stroke-linecap: round;
    stroke-linejoin: round;
    stroke-width: 1.6;
    transition: transform 160ms ease;
  }

  details[open] summary svg {
    transform: rotate(180deg);
  }

  .nav-list {
    position: absolute;
    z-index: 20;
    top: calc(100% + 0.55rem);
    right: 0;
    width: min(17rem, calc(100vw - 2rem));
    padding: 0.5rem;
    border: 1px solid var(--line);
    border-radius: 9px;
    background: var(--paper-raised);
    box-shadow: var(--shadow);
  }

  a {
    display: flex;
    min-height: 44px;
    padding: 0.5rem 0.7rem;
    align-items: center;
    border-radius: 5px;
    color: var(--ink-soft);
    font-size: 0.86rem;
    font-weight: 650;
    text-decoration: none;
  }

  a:hover,
  a[aria-current='page'] {
    background: var(--accent-soft);
    color: var(--accent-strong);
  }

  @media (min-width: 52rem) {
    .mobile-nav {
      display: none;
    }

    .desktop-nav {
      display: block;
    }

    .desktop-list {
      display: flex;
      gap: 0.1rem;
    }

    a {
      min-height: 40px;
      padding-inline: 0.58rem;
    }
  }
</style>
